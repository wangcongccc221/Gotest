# Native libraries

This directory is the HarmonyOS package location for the generated arm64-v8a
native libraries:

- `liblog.so`, built by `native/log_compat/build_ohos.sh`
- `libohos.so`, built by `go/ohos/build_ohos.sh`

The libraries are generated artifacts and are intentionally not checked in by
this scaffold.
