# 为什么有这么多等级表的页面呢？ 到底我用的是哪个文件 我都懵逼了
_Exported on 06/15/2026 at 10:56:09 GMT+8 from Claude via WayLog_


**User**

为什么有这么多等级表的页面呢？ 到底我用的是哪个文件 我都懵逼了

---


**Claude**

先帮你把所有"等级表"相关的页面找出来,看清楚到底有几个、哪个在用。

---


**Claude**

`.preview` 和 `build` 是编译缓存,忽略。真正的源码在 `entry/src/main/ets/` 下。先看清楚有哪几个"表"组件,再查谁真正在用它们。

真正的源码文件(去掉缓存)里和"等级/表"相关的有这几个:

- `components/layout/LevelTable.ets` — 等级表
- `components/tables/CompositeGradeTable.ets` — 复合等级表
- `components/tables/GradeStatisticsTable.ets` — 等级统计表
- `components/tables/HomeGradeStatsTable.ets` — 主页等级统计表
- `pages/realtime/GradeStatisticsTable.ets` — 等级统计表(又一个!)
- `pages/level/LevelDataTable.ets` — 等级数据表

查一下到底谁 import 了谁,把没用的揪出来。

---


**Claude**

很清楚了。注意一个关键点:大部分文件只是 `import { TableRow }`,`TableRow` 是个**数据类型**,不是表格组件。真正被当组件渲染(`组件名({...})`)的只有 4 个。

让我把每个表格组件被实例化的地方和死代码彻底查清楚。

---


**Claude**

分类器暂时不可用,我改用只读的 Grep 工具逐个查。

---


**Claude**

现在轮廓清楚了,但有 3 个可疑的"死表格"和一个关键疑问:**主页那个等级表到底是哪个组件渲染的?** `LevelTable` 这个 struct 居然没被实例化。再确认一遍 `GridTable`、`HomeGradeStatsTable`、`LevelTable` 的实例化情况,并看主页结构。

---


**Claude**

基本锁定了。主页的等级表其实是 **`GridTable`**(由 `DataTablesTabBar` 渲染)。在下结论前,把 3 个可疑"死表格"做最后一次全量确认——搜整个源码树的所有引用。

---
